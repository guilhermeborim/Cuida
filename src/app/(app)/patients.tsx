import { Text, View } from "react-native";

export default function Patients() {
  return (
    <View className="flex-1 items-center justify-center bg-surface">
      <Text className="text-text-primary font-bold text-heading">
        TELA DE PACIENTES
      </Text>
    </View>
  );
}
